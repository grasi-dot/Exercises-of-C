#include <stdio.h>

int main(){
	int num, soma;
	printf("Digite um valor: ");
	scanf("%d", &num);//entrada tipo int
	soma = num+5;
	printf("O valor da soma da variavel num + 5 e:%d",soma);
	return 0;
}
